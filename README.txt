Place same-math.pdf at assets/docs/same-math.pdf before pushing. Replace the root files in your site with the contents of this folder.

Fix in this package: restores missing chainContourSegments() helper in same-estimate.js, which was causing the section to open but render no content.
Use assets/docs/same-math.pdf for the paper link.
